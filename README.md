Claude Code skill to check your eToro portfolio via eToro's API and return potential growing stock and report this to the user via whatsapp. The skill/scripts can be modified to provide more or less information and/or report this to the user via a different channel. 

The API key needs to be generated directly on eToro's site and you need to import it as an environment variable sicne that's how the script is fetching it.
